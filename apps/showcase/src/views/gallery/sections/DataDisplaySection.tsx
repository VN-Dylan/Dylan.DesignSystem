import { SectionShell } from '@/views/gallery/sections/SectionShell'

/** DataDisplay category page — filled in by the gallery codex batch. */
export function DataDisplaySection() {
  return (
    <SectionShell slug="data-display">
      <p className="text-sm text-content-muted">Examples coming soon.</p>
    </SectionShell>
  )
}
