// @dylan-ds/utils — hooks and helpers.
// P0 ships only `classNames`; the remaining 31 utilities land in P1
// (16 hooks · 14 more functions · 1 HOC). See PROGRESS.md.

export { classNames, type ClassValue } from './classNames'
export { classNames as default } from './classNames'
