// @dylan-ds/ui — component library entry point.
//
// P0 ships the a11y primitive only. The "golden five" (Button, Input, Select,
// Dialog, Table) land in P1 and set the pattern every later component follows.
// Full inventory + status: PROGRESS.md.

import './styles/index.scss'

export { VisuallyHidden, type VisuallyHiddenProps } from './VisuallyHidden'
